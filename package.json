{
  "name": "@userland-run/terminal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Console — the terminal front-end for userland.run / NanoVM",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.71",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
