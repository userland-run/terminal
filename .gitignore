node_modules
dist
*.local
.DS_Store

# Generated from the nano repo (see README) — not checked in.
public/nano.wasm

# Playwright E2E artifacts (the reporter's results file is published, not committed).
/userland-results.json
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Synced from the SDK by the predev/prebuild sync-sw script — not checked in.
public/nano-sw.js

# dev-deployed nanoinfer engine + model symlink (built from ../nanoinfer)
public/nanoinfer-engine/
public/models

# Guest payloads served to the VM — generated/vendored, not source. Their
# canonical, buildable home is the catalog opencode recipe (recipes/opencode).
public/opencode/
public/npm/
