node_modules
dist
*.local
.DS_Store

# Generated from the nano repo (see README) — not checked in.
public/nano.wasm

# Playwright E2E artifacts (the reporter's results file is published, not committed).
/userland-results.json
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Synced from the SDK by the predev/prebuild sync-sw script — not checked in.
public/nano-sw.js
